//Prototyping is similar to inheritance. It should be object inheritance

let a = {}

//all objects, by default, inherit from the "Null Prototype". 
//they have a special method __proto__ that points to Null. It is a global method.

console.log(a.__proto__)