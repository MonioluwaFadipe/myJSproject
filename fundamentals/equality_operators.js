// ==  verifies just the values (normal equality check)
// === checks for values and type (strict equality check)
// != Checks if the values are not equal
// !== checks if the values and types are not equal

let a = 100
let b = "100"

console.log(a !== b)