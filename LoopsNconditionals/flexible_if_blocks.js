//Boolean data type isn't the only allowable one in If blocks. We can also pass other data types in an "If block"

let val = 100

if(val){
    console.log("This is true")
} else {
    console.log("This is false")
}
